name: Build APK with Buildozer
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Clean up disk space early
        run: |
          echo "🧹 Очистка диска..."
          sudo rm -rf /home/runner/actions-runner/_work/_temp/*
          sudo rm -rf ~/.cache/pip
          sudo rm -rf ~/.gradle
          sudo rm -rf ~/.m2
          df -h  # Проверка свободного места



      - name: Install minimal dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
              build-essential \
              python3 python3-pip git \
              openjdk-17-jdk \
              libncurses5-dev libncursesw5-dev \
              autoconf automake libtool \
              pkg-config \
              zlib1g-dev libffi-dev libssl-dev \
              cmake unzip wget
          
          pip3 install --upgrade pip
          pip3 install Cython==0.29.33 buildozer





      - name: Prepare Android SDK
        run: |
          echo "📱 Настройка Android SDK..."
          export ANDROID_HOME=$HOME/.buildozer/android/platform/android-sdk
          export ANDROID_CMDLINE_TOOLS=$ANDROID_HOME/cmdline-tools/latest
          export JAVA_HOME=/usr/lib/jvm/temurin-17-jdk-amd64
          export PATH=$JAVA_HOME/bin:$PATH

          mkdir -p $HOME/.android && touch $HOME/.android/repositories.cfg
          mkdir -p $ANDROID_HOME/licenses
          echo "8933bad161af4178b1185d1a37fbf41ea5269c55" > $ANDROID_HOME/licenses/android-sdk-license
          echo "d56f5187479451eabf01fb78af6dfcb131a6481e" >> $ANDROID_HOME/licenses/android-sdk-license

          if [ ! -d "$ANDROID_CMDLINE_TOOLS" ]; then
            cd /tmp
            wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O cmdline-tools.zip
            unzip -q cmdline-tools.zip -d cmdline-tools-temp
            mkdir -p $ANDROID_HOME/cmdline-tools/latest
            mv cmdline-tools-temp/cmdline-tools/* $ANDROID_HOME/cmdline-tools/latest/ 2>/dev/null || true
            rm -rf cmdline-tools-temp cmdline-tools.zip
          fi

          # Создаём симлинк tools → latest (для совместимости)
          [ -L "$ANDROID_HOME/tools" ] || ln -sf $ANDROID_HOME/cmdline-tools/latest $ANDROID_HOME/tools

          # Устанавливаем только нужное
          yes | $ANDROID_HOME/tools/bin/sdkmanager --sdk_root=$ANDROID_HOME --install "build-tools;34.0.0" "platforms;android-34"
        env:
          JAVA_HOME: /usr/lib/jvm/temurin-17-jdk-amd64

      - name: Clean up before build
        run: |
          echo "🧹 Освобождаем место..."
          sudo rm -rf /home/runner/actions-runner/_work/_temp/*
          df -h
          
      - name: Patch libffi to fix LT_SYS_SYMBOL_USCORE
        run: |
          echo "🔧 Патчим libffi: копируем libtool.m4 вручную..."

          # Устанавливаем libtool и ищем нужные .m4 файлы
          sudo apt-get install -y libtool libtool-bin

          # Находим файлы libtool
          LIBTOOL_M4=$(find /usr/share/aclocal -name libtool.m4 | head -n 1)
          LT_OBSOLETE_M4=$(find /usr/share/aclocal -name lt~obsolete.m4 | head -n 1)

          if [ ! -f "$LIBTOOL_M4" ]; then
            echo "❌ Не найден libtool.m4"
            exit 1
          fi

          if [ ! -f "$LT_OBSOLETE_M4" ]; then
            echo "⚠️ Не найден lt~obsolete.m4 — попробуем создать пустой"
            sudo touch /tmp/lt~obsolete.m4
            LT_OBSOLETE_M4=/tmp/lt~obsolete.m4
          fi

          # Указываем путь, куда p4a скачивает libffi
          LIBFFI_BUILD_DIR="$HOME/.buildozer/android/platform/build-arm64-v8a/build/bootstrap_builds/sdl2-python3/ffi"

          # Создаём папку m4, если её нет
          mkdir -p "$LIBFFI_BUILD_DIR/m4"

          # Копируем макросы
          cp "$LIBTOOL_M4" "$LIBFFI_BUILD_DIR/m4/"
          cp "$LT_OBSOLETE_M4" "$LIBFFI_BUILD_DIR/m4/"

          echo "✅ libtool.m4 и lt~obsolete.m4 скопированы в $LIBFFI_BUILD_DIR/m4/"

          # Проверим, что файлы есть
          ls -la "$LIBFFI_BUILD_DIR/m4/"




      - name: Fix libffi header path
        run: |
          echo "🔧 Создаём симлинк для ffi.h..."
          sudo mkdir -p /usr/include/ffi
          sudo ln -sf /usr/include/x86_64-linux-gnu/ffi.h /usr/include/ffi/ffi.h
          sudo ln -sf /usr/lib/x86_64-linux-gnu/libffi.so /usr/lib/libffi.so
          ls /usr/include/ffi/ffi.h && echo "✅ Готово"



- name: Build APK
  run: |
    echo "🔄 Запускаем сборку APK..."


    # Удаляем старый .buildozer
    rm -rf .buildozer

    buildozer android update

    LIBFFI_BUILD_DIR="$HOME/.buildozer/android/platform/build-arm64-v8a/build/bootstrap_builds/sdl2-python3/ffi"

    if [ ! -d "$LIBFFI_BUILD_DIR" ]; then
      echo "❌ Не найдена папка libffi: $LIBFFI_BUILD_DIR"
      find $HOME/.buildozer/android/platform/build-arm64-v8a/build/bootstrap_builds -type d -name "ffi" || true
      exit 1
    fi

    echo "🔧 Патчим libffi: исправленная версия"

    sudo apt-get install -y libtool libtool-bin autoconf automake

    mkdir -p "$LIBFFI_BUILD_DIR/m4"

    # Копируем все m4-файлы из системы
    cp /usr/share/aclocal/*.m4 "$LIBFFI_BUILD_DIR/m4/" 2>/dev/null || true


    # Скачиваем libtool.m4, если нет
    if [ ! -f "$LIBFFI_BUILD_DIR/m4/libtool.m4" ]; then
      wget -q https://git.savannah.gnu.org/cgit/libtool.git/plain/m4/libtool.m4 -O "$LIBFFI_BUILD_DIR/m4/libtool.m4"
    fi

    CONFIGURE_AC="$LIBFFI_BUILD_DIR/configure.ac"


    # 1. Добавляем AC_CONFIG_MACRO_DIRS в начало файла (после AC_INIT)
    if ! grep -q "AC_CONFIG_MACRO_DIRS" "$CONFIGURE_AC"; then
      sed -i '5iAC_CONFIG_MACRO_DIRS([m4])' "$CONFIGURE_AC"
    fi

    # 2. Вставляем макрос LT_SYS_SYMBOL_USCORE точно
    MACRO_DEF='AC_DEFUN([LT_SYS_SYMBOL_USCORE], [
      AC_REQUIRE([AC_CANONICAL_SYSTEM])
      AS_VAR_PUSHDEF([lt_cv_sys_symbol_uscore], [lt_cv_sys_symbol_underscore])
      AC_CACHE_CHECK([for _ in symbol names], lt_cv_sys_symbol_uscore,
        [AC_LINK_IFELSE([AC_LANG_PROGRAM([[#ifdef __cplusplus
          extern "C" void sym_underscore ();
          #endif
          void sym_underscore () {}]], [[return !sym_underscore ();]])],
          [AS_CASE([$ac_cv_host], [*[[-_]]*], [lt_cv_sys_symbol_uscore=yes], [lt_cv_sys_symbol_uscore=no])],
          [lt_cv_sys_symbol_uscore=no])])
      AC_SUBST([lt_sys_symbol_underscore], [$]1)
      AS_VAR_POPDEF([lt_cv_sys_symbol_uscore])
    ])'

    if ! grep -q "LT_SYS_SYMBOL_USCORE" "$CONFIGURE_AC"; then
      echo "$MACRO_DEF" >> "$CONFIGURE_AC"
      echo "✅ Макрос LT_SYS_SYMBOL_USCORE добавлен в конец configure.ac"
    fi

    # 3. Запускаем aclocal + autoconf вручную
    cd "$LIBFFI_BUILD_DIR"
    aclocal -I m4 || echo "⚠️ aclocal failed, но продолжаем"
    autoconf || echo "⚠️ autoconf failed, но продолжаем"


    echo "🚀 Запускаем сборку APK..."
    buildozer -v android debug






      - name: Upload APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: app-apk
          path: bin/*.apk
          retention-days: 7
